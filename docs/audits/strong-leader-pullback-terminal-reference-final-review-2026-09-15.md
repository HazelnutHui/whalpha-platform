# Strong-Leader Pullback Final Terminal-Reference Review

## Decision

The outcome-blind terminal-reference gate is complete. All 302 registered
five-session crossing paths now have either an exact reference or a finite
pre-outcome interval:

| Reference state | Paths |
| --- | ---: |
| Exact | 219 |
| Finite interval | 83 |
| Unbounded | 0 |

The 83 interval-censored paths retain a zero lower gross reference and a
source-supported finite upper reference. They cannot be point-imputed, omitted
from a headline, or silently treated as exact. Development and later validation
must repeat decisions under adverse interval endpoints.

## Bound evidence

The final Dell review formally reread the terminal V3/V4 population, retained
payoff and consideration reports, the original and supplemental SEC source
packages, and six exact canonical EOD closes. It made zero network requests and
read no forward outcome.

- report path:
  `historical-evidence/strong-leader-pullback-terminal-reference-final-review/review=20260915-v1`;
- report SHA-256:
  `5a49da2a9c148b2c237faf3b3232cd1a1de5f532c6a575645b455f3b2145bcdd`;
- logical fingerprint:
  `37e8f7adf3433649c18961a96390be7f27ee2c0009f0d84609277ca4687c1777`;
- residual population: 18 cases / 88 paths; and
- custody: directory `0700`, report `0400`, no partial or symlink residue.

An exact second execution returned `already_present` with identical hashes.

## Authority

These are terminal reference inputs, not calculated returns. The review
contains zero outcome, performance metric, parameter selection, canonical
write, or Production write. It closes only the finite-reference input gate;
research authority is decided by the separate V2 admission report.
