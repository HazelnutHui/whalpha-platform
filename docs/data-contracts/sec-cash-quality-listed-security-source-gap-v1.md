# SEC Cash-Quality Listed-Security Source Gap V1

Contract: `quant-research-sec-cash-quality-source-gap/1.0`.

This outcome-blind plan starts from ADR 0308's fixed denominator of 75,391
issuer-level TTM observations and zero fully applicable listed-security rows.
It binds applicability result fingerprint
`da67ce358fef0bb533875125d658ee6f2e365c435f17c08036d9b03d12fa578e`.
It keeps five proof lanes independent: effective-dated stable-instrument/CIK,
security form, listing interval and ticker aliases, issuer/security structure,
and the multi-common policy. A source is positive-admission capable only when
local evidence proves a stable key, effective interval, and source knowledge
time for its lane. Marketing descriptions and planned samples cannot qualify.

The registered capability inventory distinguishes retained local evidence,
free official sources, open identifier mapping, Massive Starter, and optional
commercial samples. SEC filer identity, ticker/name, and a current provider
snapshot projected backward are forbidden positive proxies.

The only first-batch route is prospective. Before any source acquisition or
outcome read, freeze an activation session after all five lanes qualify. The
denominator is every TTM-ready issuer observation on or after that session.
Admission then requires exactly one qualified common security per CIK at the
signal cutoff. ADR/ADS, foreign ordinary shares, multi-common issuers, and
unknown/conflicting structures remain quarantined; explicit non-common forms
are excluded. This selection is based only on input eligibility and cannot use
returns, coverage convenience, or later observations.

The contract performs no network request, credential read, canonical write,
projection, factor materialization, outcome access, Validation, Holdout, or
Product action.
